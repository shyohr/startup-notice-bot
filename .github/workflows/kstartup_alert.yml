name: K-Startup Final Precision Alert

on:
  schedule:
    - cron: '0 0 * * *'
  workflow_dispatch:

jobs:
  scan-and-notify:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v3

      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.9'

      - name: Install Dependencies
        run: pip install requests beautifulsoup4

      - name: Run Scraper
        env:
          DISCORD_WEBHOOK: ${{ secrets.DISCORD_WEBHOOK_URL }}
        run: |
          python -c "
          import requests
          from bs4 import BeautifulSoup
          import os
          import json
          import datetime
          import re

          def send_discord(text):
              webhook_url = os.environ.get('DISCORD_WEBHOOK')
              payload = json.dumps({'content': text}, ensure_ascii=False)
              requests.post(webhook_url, data=payload.encode('utf-8'), headers={'Content-Type': 'application/json'})

          try:
              url = 'https://www.k-startup.go.kr/web/contents/bizpbanc-ongoing.do'
              headers = {'User-Agent': 'Mozilla/5.0 (Windows NT 10.0; Win64; x64) AppleWebKit/537.36 (KHTML, like Gecko) Chrome/120.0.0.0 Safari/537.36'}
              
              res = requests.get(url, headers=headers, timeout=30, verify=False)
              soup = BeautifulSoup(res.text, 'html.parser')
              
              # 카드형 리스트 아이템 추출
              posts = soup.select('div.pbancList > ul > li') or soup.select('ul.list_type_common > li')
              
              keywords = ['예비창업패키지', '초기창업패키지', '창업도약패키지', '창업패키지', '청년창업사관학교']
              found_items = []

              for post in posts:
                  # 1. 제목 추출 및 정제
                  title_tag = post.find('a')
                  if not title_tag: continue
                  raw_title = title_tag.get_text(strip=True)
                  # '새로운게시글', '새창열기' 및 아이콘 텍스트 제거
                  clean_title = re.sub(r'(새로운게시글|새창열기|N$|NEW$)', '', raw_title).strip()
                  
                  if any(k in clean_title for k in keywords):
                      # 2. 마감일자 정밀 추출
                      # 스크린샷의 '마감일자 2026-01-27' 패턴을 찾음
                      full_text = post.get_text(' ', strip=True)
                      # '마감일자' 단어 뒤에 오는 날짜(YYYY-MM-DD)를 특정해서 추출
                      date_match = re.search(r'마감일자\s*(\d{4}-\d{2}-\d{2})', full_text)
                      
                      if date_match:
                          date_val = date_match.group(1) # 찾은 날짜 그대로 사용
                      else:
                          # 패턴이 안 보일 경우 일반 날짜 형식이라도 검색
                          simple_match = re.search(r'(\d{4}-\d{2}-\d{2})', full_text)
                          date_val = simple_match.group(0) if simple_match else '기간상세참조'
                      
                      found_items.append({'title': clean_title, 'date': date_val})

              # 3. 메시지 조립
              today = datetime.date.today().strftime('%Y-%m-%d')
              
              if not found_items:
                  message = f'**[{today} 맞춤형 창업 지원 공고]**\n\n현재 모집 중인 관련 공고가 없습니다.'
              else:
                  message = f'**[{today} 맞춤형 창업 지원 공고]**\n\n'
                  for item in found_items:
                      message += f'📌 {item[\"title\"]}\n'
                      message += f'마감일자 {item[\"date\"]}\n\n'
                  
                  message += f'모집공고: {url}'

              send_discord(message)

          except Exception as e:
              send_discord(f'⚠️ 스크립트 에러 발생: {str(e)}')
          "
